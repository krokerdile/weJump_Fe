import DefaultButton from "./DefaultButton";

const GoogleLoginButton = ({ onClick }) => {
  return <DefaultButton onClick={onClick} text="Continue with Goolge" background="grey1" />;
};

export default GoogleLoginButton;
